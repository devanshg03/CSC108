def area(base: float, height: float) -> float:
    """Returns the area of a triangle using the given base and height.
    
    >>> area(10,5)
    25.0
    >>> area(2.2,2.4)
    2.464
    """
    return base * height / 2